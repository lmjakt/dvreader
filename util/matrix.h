#ifndef MATRIX_H
#define MATRIX_H

// some functions to handle matrices. No thread safety considered.

// add values of b to values of a.
void increment_matrix(float* a, float* b, unsigned int l); 

#endif
